{
  "_comment": "Spaces uses S3-compatible bucket-policy syntax. Apply with: aws s3api put-bucket-policy --bucket <name> --policy file://policy.json --endpoint-url=https://<region>.digitaloceanspaces.com",
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "DenyInsecureTransport",
      "Effect": "Deny",
      "Principal": "*",
      "Action": "s3:*",
      "Resource": [
        "arn:aws:s3:::REPLACE_BUCKET_NAME",
        "arn:aws:s3:::REPLACE_BUCKET_NAME/*"
      ],
      "Condition": {
        "Bool": {
          "aws:SecureTransport": "false"
        }
      }
    },
    {
      "Sid": "AllowCDNReadOnly",
      "Effect": "Allow",
      "Principal": "*",
      "Action": "s3:GetObject",
      "Resource": "arn:aws:s3:::REPLACE_BUCKET_NAME/public/*",
      "_note": "Only the /public/ prefix is world-readable; everything else requires signed URLs."
    }
  ]
}
