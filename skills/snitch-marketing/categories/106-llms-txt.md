## CATEGORY 106: llms.txt — merged into Category 82

The `llms.txt` audit — presence, format, content type, linked-URL resolution, `llms-full.txt` drift, provenance — now runs as Layer 1 (Discoverability) of `categories/82-ai-search-citation.md`, which carries the evidence-based posture: a missing file is Medium at most and deducts once, while a present-but-misleading file is the stronger finding.

**Do not scan this file.** If category 106 is selected, scan Category 82 instead and report the finding under it.
