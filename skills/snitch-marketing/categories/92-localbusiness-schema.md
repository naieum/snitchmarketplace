## CATEGORY 92: LocalBusiness schema — merged into Category 32

LocalBusiness (and its subtypes) validation now runs as one row of `categories/32-schema-type-validation.md`, which asks the
same three questions of every schema type. That row's page-type signal, required and recommended
properties, rich-result status, pitfalls and Fix voice live in `references/standards-table.md`.

**Do not scan this file.** If category 92 is selected, scan Category 32 instead and report the finding under it.
