## CATEGORY 108: UTM hygiene — merged into Category 53

The UTM convention audit — casing drift, missing parameters, double-encoding, redirect stripping, internal-nav propagation, UTM-dirty canonicals, email-template drift — now runs as the UTM pass of `categories/53-analytics-instrumentation.md`, over the same source the install and taxonomy passes read.

**Do not scan this file.** If category 108 is selected, scan Category 53 instead and report the finding under it.
