## CATEGORY 103: Accessibility conformance (WCAG 2.2 AA + legal exposure) — moved to snitch-ada

The WCAG 2.2 Level AA criterion table, the pass sequence, the contrast evidence format and the legal-exposure read now run across categories 01-14 of the accessibility skill, which judges the criterion and the regime rather than search and traffic.

**Do not scan this file.** If this category is selected, call the Skill tool with "snitch-ada" and report the finding there. Marketing still audits the same elements where the judge is search: image alt as a search signal (Cats 25, 26), viewport (Cat 45), `lang` as a machine-readability signal (Cat 52).
