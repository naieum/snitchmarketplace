## CATEGORY 64: Email design + accessibility — merged into Category 61

Rendering and accessibility checks (layout primitive, inline CSS vs stripped `<style>` blocks, image alt text, preview text, plain-text alternative, container width, dark mode) now run as the design half of `categories/61-email-inventory.md`, over the same template inventory.

**Do not scan this file.** If this category is selected, scan Category 61 instead (`categories/61-email-inventory.md`) and report the finding under it.
