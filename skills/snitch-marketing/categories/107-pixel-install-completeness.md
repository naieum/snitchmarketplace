## CATEGORY 107: Pixel install completeness — moved to snitch-adsready

The cross-platform pixel inventory — which ad-platform pixels are installed, where each fires, whether each is consent-gated and whether each has a server-side/Conversions-API pair — now runs in the ads-readiness skill, which can also verify firing against the platform APIs.

**Do not scan this file.** If category 107 is selected, call the Skill tool with "snitch-adsready" and report the pixel inventory from there. Marketing still reports the presence signal it can see in the source (Cat 66 for paid channel presence, Cat 53 for the analytics layer).
