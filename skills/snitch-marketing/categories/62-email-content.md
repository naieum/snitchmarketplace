## CATEGORY 62: Email content quality — merged into Category 61

Template copy checks (stale "beta" labels, merge tags rendering literally, placeholder values, localhost / preview URLs in CTAs, dead CTA destinations, voice violations against the project's own guide) now run as the content half of `categories/61-email-inventory.md`, over the same template inventory.

**Do not scan this file.** If this category is selected, scan Category 61 instead (`categories/61-email-inventory.md`) and report the finding under it.
