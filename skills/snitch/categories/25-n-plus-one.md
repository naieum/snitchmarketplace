## CATEGORY 25: N+1 Queries — deprecated

Query-count inefficiency is a performance defect, not attacker impact, and this skill's own
description excludes general code review unrelated to security. Where an unbounded, user-triggered
query fan-out is genuinely a denial-of-service surface, scan Category 7 (Rate Limiting) instead.

**Do not scan this file.** If category 25 is selected, say the category is deprecated and offer the active categories instead.
