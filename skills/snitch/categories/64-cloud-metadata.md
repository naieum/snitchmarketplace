## CATEGORY 64: Cloud Metadata Endpoint Exploitation — merged into Category 5

This category was consolidated into `categories/05-ssrf.md` (SSRF), which now carries the
resolved-IP denylist, redirect re-check, DNS-rebinding, and metadata-target guidance that lived
here. The instance-level controls — IMDSv2 enforcement and egress NetworkPolicy — moved to
Category 43 (IaC Security).

**Do not scan this file.** If category 64 is selected, scan Category 5 instead and report findings under it.
