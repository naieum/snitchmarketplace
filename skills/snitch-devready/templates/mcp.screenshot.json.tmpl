{
  "//": "Generated by claude-ready for UI projects: a screenshot/browser MCP so Claude can SEE its rendered output and iterate (Boris' feedback-loop tip). Pick ONE server and delete the other. Check this file in so the whole team gets the loop.",
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"]
    },
    "puppeteer": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-puppeteer"]
    }
  }
}
