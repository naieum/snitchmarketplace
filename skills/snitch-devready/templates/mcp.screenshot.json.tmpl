{
  "//": "Generated by snitch-devready for UI projects: a screenshot/browser MCP so the agent can SEE its rendered output and iterate (the feedback-loop tip). Check this file in so the whole team gets the loop.",
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"]
    }
  }
}
