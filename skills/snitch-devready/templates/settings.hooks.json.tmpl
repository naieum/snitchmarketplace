{
  "//": "Generated by snitch-devready. MERGE the hooks block into .claude/settings.json (checked in, whole team) or .claude/settings.local.json (personal) — never overwrite. Replace each command with the repo's real gate command from `devready.sh standards`; delete entries for layers the repo doesn't have. PostToolUse runs after every Edit/Write (keep it fast — a file-scoped lint, not the whole suite); Stop runs when Claude finishes a turn (the fuller check).",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          { "type": "command", "command": "{{LINT_ON_EDIT_CMD}}" }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          { "type": "command", "command": "{{VERIFY_CMD}}" }
        ]
      }
    ]
  }
}
